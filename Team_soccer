-- ====================================================================
-- SCRIPT XOẠC: ĐÓN LÕNG BÓNG (PREDICTION) + HẠ GẦM 1 STUD (FULL CODE)
-- ====================================================================

-- Xóa giao diện cũ nếu có để tránh trùng lặp
if game.CoreGui:FindFirstChild("CustomSlideUI") then
    game.CoreGui.CustomSlideUI:Destroy()
end

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "CustomSlideUI"
ScreenGui.Parent = game.CoreGui
ScreenGui.ResetOnSpawn = false

local SlideButton = Instance.new("TextButton")
SlideButton.Name = "CustomSlideButton"
SlideButton.Parent = ScreenGui
SlideButton.Size = UDim2.new(0, 70, 0, 70)
SlideButton.Position = UDim2.new(1, -90, 0.5, 0) 
SlideButton.BackgroundColor3 = Color3.fromRGB(255, 60, 60)
SlideButton.Text = "SLIDE"
SlideButton.TextColor3 = Color3.fromRGB(255, 255, 255)
SlideButton.TextSize = 16
SlideButton.Font = Enum.Font.SourceSansBold
SlideButton.Active = true

-- Bo tròn thành hình tròn hoàn toàn
local ButtonCorner = Instance.new("UICorner")
ButtonCorner.CornerRadius = UDim.new(1, 0)
ButtonCorner.Parent = SlideButton

local isLocked = false
local isDragging = false

-- Hàm kéo thả nút di động
local function makeDraggable(frame)
    local UserInputService = game:GetService("UserInputService")
    local dragInput, dragStart, startPos
    frame.InputBegan:Connect(function(input)
        if (input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch) then
            if isLocked then return end
            isDragging = true
            dragStart = input.Position
            startPos = frame.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then isDragging = false end
            end)
        end
    end)
    frame.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then dragInput = input end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if input == dragInput and isDragging and not isLocked then
            local delta = input.Position - dragStart
            frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
end
makeDraggable(SlideButton)

-- Cơ chế nhấn giữ khóa vị trí
local holdTime = 1
local holding = false
local holdTimer = 0
local holdCoroutine = nil

SlideButton.MouseButton1Down:Connect(function()
    if isDragging then return end
    holding = true
    holdTimer = 0
    if holdCoroutine then task.cancel(holdCoroutine) end
    holdCoroutine = task.spawn(function()
        local currentTimer = 0
        while holding do
            task.wait(0.1)
            if not holding then break end
            currentTimer = currentTimer + 0.1
            holdTimer = currentTimer
            if currentTimer >= holdTime then
                isLocked = not isLocked
                SlideButton.BackgroundColor3 = isLocked and Color3.fromRGB(60, 255, 60) or Color3.fromRGB(255, 60, 60)
                holding = false
                break
            end
        end
    end)
end)

local function cancelHold()
    holding = false
    if holdCoroutine then task.cancel(holdCoroutine); holdCoroutine = nil end
end
SlideButton.MouseButton1Up:Connect(cancelHold)
SlideButton.MouseLeave:Connect(cancelHold)

----------------------------------------------------
-- LOGIC XOẠC: ĐÓN LÕNG BÓNG (PREDICTION) + HẠ GẦM 1 STUD
----------------------------------------------------
local isCooldown = false

SlideButton.MouseButton1Click:Connect(function()
    if holdTimer >= holdTime then holdTimer = 0 return end
    if isCooldown then return end

    local ball = workspace:FindFirstChild("BallsInPlay") and workspace:FindFirstChild("BallsInPlay"):FindFirstChild("Ball")
    local player = game.Players.LocalPlayer
    local char = player.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    local hum = char and char:FindFirstChildOfClass("Humanoid")
    
    if ball and hrp then
        local distance = (hrp.Position - ball.Position).Magnitude
        
        if distance <= 30 then
            isCooldown = true
            local anchorPosition = ball.Position
            
            -- Thuật toán dự đoán vị trí bóng (bù trễ cho máy lag)
            local predictionTime = 0.1 
            local predictedBallPos = ball.Position + (ball.AssemblyLinearVelocity * predictionTime)
            
            -- Tính toán hướng di chuyển thực tế của bóng
            local ballMoveDir = ball.AssemblyLinearVelocity
            local direction = (ballMoveDir.Magnitude > 1) and ballMoveDir.Unit or hrp.CFrame.LookVector
            direction = Vector3.new(direction.X, 0, direction.Z).Unit
            
            -- Điểm đón: Cách vị trí dự đoán 7 studs về phía trước, hạ gầm xuống 1 stud
            local targetPos = predictedBallPos + (direction * 7)
            targetPos = Vector3.new(targetPos.X, ball.Position.Y + 1, targetPos.Z)
            
            -- Đổi hướng nhân vật nhìn cùng chiều bóng lăn
            hrp.CFrame = CFrame.new(hrp.Position, hrp.Position + direction)
            
            -- Lướt tới tốc độ cao (180) để bắt kịp bóng nhanh chóng
            local bodyVel = Instance.new("BodyVelocity")
            bodyVel.MaxForce = Vector3.new(100000, 100000, 100000)
            bodyVel.Velocity = (targetPos - hrp.Position).Unit * 180
            bodyVel.Parent = hrp
            
            local dashTime = tick()
            while tick() - dashTime < 0.2 do
                if (ball.Position - anchorPosition).Magnitude > 30 then break end
                task.wait(0.05)
            end
            
            bodyVel:Destroy()
            
            -- Kích hoạt lệnh xoạc
            game:GetService("ReplicatedStorage"):WaitForChild("Events"):WaitForChild("Slide"):FireServer(ball, 0.5, false)
            
            -- Ghim bám dính
            local att0 = Instance.new("Attachment", hrp)
            local att1 = Instance.new("Attachment", ball)
            local alignPos = Instance.new("AlignPosition", hrp)
            alignPos.Attachment0 = att0
            alignPos.Attachment1 = att1
            alignPos.MaxForce = 999999 
            alignPos.Responsiveness = 150
            
            local lockTime = tick()
            while tick() - lockTime < 0.6 do
                if (ball.Position - anchorPosition).Magnitude > 30 then break end
                task.wait(0.05)
            end
            
            att0:Destroy()
            att1:Destroy()
            alignPos:Destroy()
            
            if hrp then hrp.AssemblyLinearVelocity = Vector3.new(0, 0, 0) end
            if hum then hum:ChangeState(Enum.HumanoidStateType.Running) end
            
            task.wait(0.2)
            isCooldown = false
        else
            -- Xoạc thường nếu ngoài tầm 30 studs
            isCooldown = true
            game:GetService("ReplicatedStorage"):WaitForChild("Events"):WaitForChild("Slide"):FireServer(ball, 0.5, false)
            task.wait(0.5)
            isCooldown = false
        end
    end
end)
